# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db

# Temporary files
*.log
*.tmp
*.bak
*~

# Secrets (contains API tokens) — real values live on the Pi / core runtime,
# not in this monorepo. Keep only config_secrets.template.json in git.
config_secrets.json

# Secrets (plugin-level)
plugins/**/config_secrets.json

# OAuth / local auth artifacts (Google Calendar and similar)
**/credentials.json
**/token.pickle
**/*.pickle

# Asset backups (local working copies)
plugins/*/assets/**/*_backup/
