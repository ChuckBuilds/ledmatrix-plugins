{
  "id": "ledmatrix-elections",
  "name": "Election Results",
  "version": "1.3.1",
  "author": "rpierce99",
  "description": "Live election results: a scrolling ticker of important races plus a full-screen interrupt when a race is newly called. Auto-activates for your state's primary + general (dormant otherwise), drains stale calls from the ticker, and survives restarts. Filterable to your state. NYT baseline provider + optional California SoS county/city rollup.",
  "entry_point": "manager.py",
  "class_name": "ElectionPlugin",
  "category": "info",
  "tags": [
    "elections",
    "news",
    "results",
    "ticker",
    "live"
  ],
  "display_modes": [
    "election_ticker",
    "election_called"
  ],
  "dependencies": [
    "cache_manager"
  ],
  "versions": [
    {
      "version": "1.3.1",
      "released": "2026-09-16",
      "ledmatrix_min_version": "3.4.0",
      "notes": "Settings description only. scroll_speed was described as pixels per frame; it is pixels per scroll_delay step, and the ticker runs at scroll_speed / scroll_delay pixels per second (33.3 by default), as the README already said. No behaviour change. Requires LEDMatrix core 3.4.0, as before."
    },
    {
      "released": "2026-09-15",
      "version": "1.3.0",
      "changelog": "Requires LEDMatrix core 3.4.0 or newer. The ticker passes frame_hold to display_manager.set_scrolling_state(), which core 3.3.x does not accept; the call used to sit in a try that swallowed the TypeError, so on those cores the ticker drew but never marked itself as scrolling. The try is gone and the floor (versions[0] and compatible_versions) is raised instead, so the store asks users on an older core to update LEDMatrix first. Scroll pacing now comes only from the core's resolver: the set_target_fps(100) and frame-based speed writes that ran around it at startup and on every config save are removed, scroll_delay defaults to 0.03 in code as the schema and README already said (it was 0.01, three times faster, for a config section missing the key; a normal load merges the schema default, so saved configs already had 0.03), and the one-pass duration uses the speed the resolver actually applied. The ticker now releases the scroll state when it has nothing to show and while a race-called card is up, instead of leaving it to the core's inactivity timeout, and when drawing a frame raises.",
      "ledmatrix_min_version": "3.4.0"
    },
    {
      "released": "2026-09-07",
      "version": "1.2.4",
      "changelog": "Resolve scroll speed through the shared resolver and apply the frame hold it reports, so slow speeds move whole pixels",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-09-03",
      "version": "1.2.3",
      "changelog": "Render text 1-bit (no anti-aliasing) so glyphs stay crisp on the LED grid",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "version": "1.2.2",
      "released": "2026-08-19",
      "ledmatrix_min_version": "2.0.0",
      "notes": "Rebuild the ticker when something else clears its scroll cache. _display_ticker() gated on its own _scroll_ready flag, which Vegas cannot clear when it invalidates the cached scroll image, so the mode could yield an empty frame while still holding race data.",
      "changes": [
        "Fixed the bundled test_mode fixtures failing to load on Windows, which left every panel blank.",
        "Documented the twelve remaining settings and replaced the placeholder screenshot with real renders of both display modes."
      ]
    },
    {
      "version": "1.2.1",
      "released": "2026-08-19",
      "ledmatrix_min_version": "2.0.0",
      "notes": "Rebuild the ticker when something else clears its scroll cache. _display_ticker() gated on its own _scroll_ready flag, which Vegas cannot clear when it invalidates the cached scroll image, so the mode could yield an empty frame while still holding race data."
    },
    {
      "version": "1.2.0",
      "released": "2026-07-31",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-07-08",
      "version": "1.1.1",
      "notes": "Add a GPL-3.0 LICENSE file (project-standard license; this plugin previously shipped without one).",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-06-15",
      "version": "1.1.0",
      "notes": "Apply config changes live via on_config_change (state, filters, providers, scroll speed); rebuilds providers and the race store and re-fetches so edits take effect without a display restart.",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-06-06",
      "version": "1.0.2",
      "ledmatrix_min": "2.0.0",
      "notes": "Rename the internal data_model module to election_data_model so it can't collide with another plugin's identically-named module. The loader puts plugin modules on sys.path by bare name; a deferred import (providers/__init__) could otherwise bind a different plugin's data_model and fail to load."
    },
    {
      "released": "2026-06-05",
      "version": "1.0.1",
      "ledmatrix_min": "2.0.0",
      "notes": "Fix the called-race interrupt looping forever: give colliding local races (e.g. multiple 'Mayor' races) a stable id derived from NYT's per-race id instead of an order-dependent counter, so the newly-called diff no longer re-fires every fetch."
    },
    {
      "released": "2026-06-03",
      "version": "1.0.0",
      "ledmatrix_min": "2.0.0"
    }
  ],
  "last_updated": "2026-09-16",
  "stars": 0,
  "downloads": 0,
  "verified": false,
  "screenshot": "",
  "license": "GPL-3.0",
  "config_schema": "config_schema.json",
  "compatible_versions": [
    ">=3.4.0"
  ]
}
