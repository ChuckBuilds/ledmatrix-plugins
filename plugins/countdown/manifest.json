{
  "id": "countdown",
  "name": "Countdown Display",
  "version": "3.3.0",
  "author": "ChuckBuilds",
  "description": "Create and manage customizable countdowns with images. Perfect for birthdays, holidays, events, and special occasions.",
  "entry_point": "manager.py",
  "class_name": "CountdownPlugin",
  "category": "custom",
  "tags": [
    "countdown",
    "timer",
    "events",
    "custom",
    "images"
  ],
  "display_modes": [
    "countdown"
  ],
  "compatible_versions": [
    ">=2.0.0"
  ],
  "versions": [
    {
      "version": "3.3.0",
      "released": "2026-09-02",
      "ledmatrix_min_version": "2.0.0",
      "notes": "Make every font in the picker actually work. Only two of the five offered families rendered: tiny and picopixel have no font file anywhere, and every bitmap family fell back silently because a .bdf exists at exactly one pixel size and the loader asked for font_size. Bitmap faces are now loaded at the size the file's PIXEL_SIZE header declares, and bitmap families use the plugin's own loader even when the core's catalog claims them -- the core resolver degrades to the default face without signalling it, which is why five_by_seven silently did nothing. tom_thumb is wired up (the file was always shipped, just unmapped), and the enum now lists the four families that verifiably render distinctly: press_start, four_by_six, five_by_seven, tom_thumb. Also rewrites the README around real rendered screenshots and documents all 40 settings, including the countdown-value ladder, which the old README got wrong: there is no '1 Day' and no 'TODAY!' string -- between one and two days out the plugin writes 'Tomorrow', and a passed countdown reads 'Nd ago' and is hidden unless show_expired is on."
    },
    {
      "version": "3.2.1",
      "released": "2026-08-13",
      "ledmatrix_min_version": "2.0.0",
      "notes": "Let a countdown save when its layout and style sections were never opened. The array-table editor keeps them in a hidden input, which comes back null or empty, and both were declared as strict objects -- so the config save failed with HTTP 400. Same defect reported against soccer-scoreboard as being unable to add a custom league; it belongs to the editor rather than to either plugin."
    },
    {
      "version": "3.2.0",
      "released": "2026-07-31",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-07-28",
      "version": "3.1.4",
      "notes": "Fix config save failing with a 400 'Expected type string, got NoneType' when a countdown has no image. image_path is optional, so the schema now allows null (type: [string, null]) alongside a path string; the manager already coerces null to an empty string at runtime.",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-07-17",
      "version": "3.1.3",
      "notes": "Config schema reorganized for a clearer setup experience: fine-tuning fields moved behind the web UI's collapsed Advanced Settings section (x-advanced). UX-only; no fields, defaults, or behavior changed.",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-07-13",
      "version": "3.1.2",
      "notes": "Fix a blank-screen bug: _resolve_font() returned None (silently skipping all text) whenever the core's font_manager was unavailable or a lookup failed, with no fallback. It now falls back to PIL's default font, so a countdown's name/value are always visible even if the configured font can't be resolved.",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-07-08",
      "version": "3.1.1",
      "notes": "Rewrite the README config section as a per-key table covering the v3 keys (target_time, mode, layout_preset, text_align, and the per-countdown style/layout overrides), add a title to the top-level enabled option, normalize the versions changelog key to ledmatrix_min_version, and set the manifest license to GPL-3.0 (project standard; the LICENSE file was already GPL).",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-05-30",
      "version": "3.1.0",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-05-30",
      "version": "3.0.1",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-05-30",
      "version": "3.0.0",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-05-15",
      "version": "2.0.1",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-03-06",
      "version": "2.0.0",
      "ledmatrix_min_version": "2.0.0"
    },
    {
      "released": "2026-03-06",
      "version": "1.0.2",
      "ledmatrix_min_version": "2.0.0"
    }
  ],
  "update_interval": 30,
  "default_duration": 15,
  "icon": "fa-clock",
  "license": "GPL-3.0",
  "homepage": "https://github.com/ChuckBuilds/ledmatrix-plugins/tree/main/plugins/countdown",
  "config_schema": "config_schema.json",
  "last_updated": "2026-09-02"
}
