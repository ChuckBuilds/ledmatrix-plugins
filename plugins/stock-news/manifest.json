{
  "id": "stock-news",
  "name": "Stock News Ticker",
  "version": "2.4.4",
  "author": "ChuckBuilds",
  "description": "Live stock headlines via Yahoo Finance search API with RSS fallback, company logos, configurable display styles (logo+ticker, ticker only, logo only), Vegas scroll integration, and per-day/hour request budgeting",
  "entry_point": "manager.py",
  "class_name": "StockNewsTickerPlugin",
  "update_interval": 30,
  "category": "financial",
  "tags": [
    "stock",
    "news",
    "financial",
    "market",
    "ticker",
    "headlines",
    "rss",
    "scrolling"
  ],
  "display_modes": [
    "stock_news_ticker"
  ],
  "repo": "https://github.com/ChuckBuilds/ledmatrix-plugins",
  "branch": "main",
  "plugin_path": "plugins/stock-news",
  "versions": [
    {
      "released": "2026-07-08",
      "version": "2.4.4",
      "notes": "Eager fetch on startup (eager_fetch_on_startup, default on) fills every configured symbol at update() cadence instead of trickling one per update_interval/n, so the ticker isn't sparse for minutes after a restart or config change. Also added manifest update_interval=30 (was defaulting to the core's 60s fallback since this plugin never declared one) so that cadence is itself twice as fast. Publisher and age are now independent segments with their own font_size/font_path/color (publisher_font_size, publisher_font_path, age_font_size, age_font_path, age_color — publisher_color no longer applies to age). Stock price can now be colored green/red by movement since market open via price_color_mode='change' (price_up_color/price_down_color), toggleable — default remains 'fixed' (text_color) for unchanged behavior.",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-07-08",
      "version": "2.4.3",
      "notes": "Set self.enable_scrolling = True so the core display loop uses its 125 FPS high-FPS path instead of the 1 FPS default path — without this flag the ticker's frame-based scroll math (tuned for ~100 FPS) only got ~1 real frame/second, making it scroll at a fraction of the configured speed in visibly choppy jumps.",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-07-08",
      "version": "2.4.2",
      "notes": "Default scroll_pixels_per_second raised from 25 to 60 — the old default made the ticker 2-4x slower than sibling ticker plugins (odds-ticker: 50, news/ledmatrix-stocks: ~100 effective) and was never retuned when frame-based scrolling was introduced in 2.3.4.",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-22",
      "version": "2.4.1",
      "notes": "custom_feeds name minLength:1 to prevent blank keys; scroll FPS reduced instead of px/frame bumped when pps is low, preserving requested speed; on_config_change detects URL edits under same feed name and clears stale cached data",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-21",
      "version": "2.4.0",
      "notes": "config_schema: fix web UI rendering — max_headlines_per_symbol now type:integer; custom_feeds changed to array-table (name+url rows); color fields changed to hex string for color-picker widget. Manager accepts both new and legacy config formats.",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-20",
      "version": "2.3.4",
      "notes": "Frame-based scrolling for smooth 1-pixel-per-frame movement; configurable max_headline_length (default 120) to reduce premature '...' truncation",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "2.3.3",
      "notes": "Fix _record_request() to count 4xx/5xx responses not just 200s; fix BRK-B/BF-B excluded by overly broad crypto filter; sanitize symbol to safe filename before filesystem operations",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "2.3.2",
      "notes": "sync_with_stocks_plugin option to mirror ledmatrix-stocks watchlist; defusedxml for RSS parsing; Optional[str] type hint on display(); fix schema descriptions (update_interval → update_interval_seconds); fix manifest/registry descriptions to reflect YF search API + RSS fallback",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "2.3.1",
      "notes": "Fix logo scaling: auto logo_size now equals display_height (not capped at 32); logos constrained by height only so landscape logos render at full panel height; failed downloads remembered per session to avoid repeated attempts",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "2.3.0",
      "notes": "Auto font size; configurable item_gap; spread symbol fetches across update_interval; market-hours throttling; budget adequacy warning; show_price; show_age with relative timestamps; publisher_color; per-symbol max_headlines dict; stale-data colour dimming; centered no-news fallback; multi-segment colour rendering",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "2.2.0",
      "notes": "on_config_change() live reload; get_display_duration() uses ScrollHelper dynamic duration; Vegas content caching; session.close() in cleanup; font_path config with 3-level fallback; _rotate_headlines() with logging; expanded get_info(); schema section titles and x-widget hints",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "2.1.0",
      "notes": "YF search API (publisher, timestamp) with RSS fallback; retry session with backoff; shuffle_headlines config; show_publisher config",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "2.0.0",
      "notes": "Real Yahoo Finance RSS fetching per symbol; company logo rendering (logo_and_ticker / ticker_only / logo_only); per-day and per-hour request budgeting; Vegas scroll integration; headline rotation; fully configurable display style, logo size, and scroll settings",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-19",
      "version": "1.1.0",
      "notes": "Implement ScrollHelper-based horizontal scrolling; yellow symbol, green headline; proper Press Start 2P font rendering",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-18",
      "version": "1.0.5",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-15",
      "version": "1.0.4",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2026-05-15",
      "version": "1.0.3",
      "ledmatrix_min": "2.0.0"
    },
    {
      "released": "2025-10-19",
      "version": "1.0.2",
      "ledmatrix_min_version": "2.0.0"
    }
  ],
  "last_updated": "2026-07-08",
  "stars": 0,
  "downloads": 0,
  "verified": true,
  "screenshot": "",
  "dependencies": {
    "managers": [
      "cache_manager"
    ]
  },
  "compatible_versions": [
    ">=2.0.0"
  ]
}
